# FastAPI and server (modern versions with Python 3.14 support and pre-built wheels)
fastapi==0.115.0
uvicorn==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36
# psycopg2-binary==2.9.9  # Not needed for SQLite
alembic==1.14.0

# Pydantic v2 (should have pre-built wheels for Python 3.14 by now)
pydantic==2.9.2
pydantic-settings==2.6.1

# AI/ML
anthropic==0.18.1

# Audio processing (simplified - removed librosa which requires complex compilation)
pydub==0.25.1

# File handling
aiofiles==23.2.1

# Environment
python-dotenv==1.0.0

# Utilities (simplified - removed packages requiring compilation)
python-dateutil==2.8.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
