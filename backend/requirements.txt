# FastAPI and server (using versions fully compatible with Pydantic v1)
fastapi==0.88.0
uvicorn==0.20.0
python-multipart==0.0.5

# Database
sqlalchemy==2.0.25
# psycopg2-binary==2.9.9  # Not needed for SQLite
alembic==1.13.1

# Pydantic (v1 for Windows compatibility - no Rust compilation needed)
pydantic==1.10.13

# AI/ML
anthropic==0.18.1

# Audio processing (simplified - removed librosa which requires complex compilation)
pydub==0.25.1

# File handling
aiofiles==23.2.1

# Environment
python-dotenv==1.0.0

# Utilities (simplified - removed packages requiring compilation)
python-dateutil==2.8.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
